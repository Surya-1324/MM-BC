import React from 'react';

const MagnetLengthDrawer = () => {
    return (
        <div className="slider-content">
            <p>
                It is typical to refer to magnet length as the dimension aligned with magnetization,
                even though this tends to be the smallest dimension (thickness) of a magnet block.
            </p>
            {/* 
            <img src="./assets/images/Wheel Radius (m).png" alt="Battery Voltage Image" style={{ width: '100%', maxWidth: '500px' }} />
            */}
            {/* Include your images, tables, etc. */}
        </div>
    );
};

export default MagnetLengthDrawer;
